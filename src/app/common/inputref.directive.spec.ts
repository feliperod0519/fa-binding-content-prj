import { InputrefDirective } from './inputref.directive';

describe('InputrefDirective', () => {
  it('should create an instance', () => {
    const directive = new InputrefDirective();
    expect(directive).toBeTruthy();
  });
});
