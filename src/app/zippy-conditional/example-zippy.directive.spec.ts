import { ExampleZippyDirective } from './example-zippy.directive';

describe('ExampleZippyDirective', () => {
  it('should create an instance', () => {
    const directive = new ExampleZippyDirective();
    expect(directive).toBeTruthy();
  });
});
